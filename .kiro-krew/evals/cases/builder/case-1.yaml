name: simple-command-implementation
description: "Test builder's ability to implement a straightforward feature"
agent: builder
input: |
  Implement a status command for the TUI based on the following specification:
  
  Task: Add StatusCmd to internal/tui/commands.go that displays active agents
  - Create handleStatus() method that calls m.manager.List()
  - Format output as table: Issue | Agent | Status | Elapsed
  - Add "status" case to executeCommand switch in internal/tui/tui.go
setup:
  - type: file
    label: "Issue Context"
    content: "file://.kiro-krew/evals/fixtures/status-command-issue.md"
  - type: text
    label: "Existing Pattern"
    content: "Other commands follow pattern: handleCommandName() method + switch case in executeCommand()"
context:
  - "Should follow existing code patterns"
  - "Must handle empty agent list gracefully"
  - "Table formatting should be clean and readable"
  - "Code should compile and pass basic tests"
expected_output: |
  Implementation including:
  - handleStatus() method in internal/tui/commands.go
  - Table formatting with proper column alignment
  - "No agents running" message when list is empty
  - Integration into executeCommand switch
  - Verification that code compiles successfully