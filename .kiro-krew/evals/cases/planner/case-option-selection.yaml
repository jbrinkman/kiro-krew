name: option-selection-format
description: "Test that planner presents options in structured format without overwhelming users"
input: |
  I want to add a caching layer to improve performance, but I'm not familiar with the different caching strategies available.
output: |
  I can help you plan a caching implementation. First, let me understand your use case better.

  What type of data are you looking to cache?
  a) API responses (reduce external call latency)
  b) Database query results (reduce DB load)
  c) Computed values (avoid repeated processing)
  d) Other (please specify)
