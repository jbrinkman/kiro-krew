name: validate-feature
description: "Evaluate validator output for checking a feature implementation"
input: |
  ## Issue
  Add a status command showing active agents.

  ## Acceptance Criteria
  - [ ] `status` command exists and is callable
  - [ ] Output shows issue number, agent name, elapsed time
  - [ ] Handles empty state gracefully

  ## Implementation
  Files changed: internal/tui/commands.go, internal/tui/tui.go
output: |
  ## Validation Report

  **Task**: Add status command showing active agents
  **Status**: ✅ PASS

  **Checks Performed**:
  - [x] `status` command exists in executeCommand switch — passed
  - [x] handleStatus function formats table with Issue, Title, Status, Elapsed columns — passed
  - [x] Empty state returns "No agents running" message — passed

  **Files Inspected**:
  - internal/tui/commands.go — handleStatus function present, correctly calls manager.List()
  - internal/tui/tui.go — status case registered in executeCommand

  **Commands Run**:
  - `go build ./cmd/kiro-krew` — exit code 0
  - `go test ./internal/tui/...` — exit code 0, all tests pass
  - `grep -n "handleStatus" internal/tui/commands.go` — found at line 58

  **Summary**: All acceptance criteria met. The status command correctly displays agent information in a formatted table and handles the empty state with a user-friendly message.
